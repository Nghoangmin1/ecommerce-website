@model Fahasa.Models.Author

@{
    ViewData["Title"] = "Chi Tiết Tác Giả";
}

<div class="d-flex align-items-center justify-content-between mb-4">
    <h4 class="mb-0 text-gray-800 fw-bold">Chi Tiết Tác Giả</h4>
    <a asp-action="Index" class="btn btn-outline-secondary">
        <i class="bi bi-arrow-left me-1"></i> Quay lại
    </a>
</div>

<div class="row">
    <div class="col-lg-8 mx-auto">
        <div class="card shadow mb-4">
            <div class="card-header py-3 bg-transparent border-bottom">
                <h6 class="m-0 font-weight-bold text-primary">Thông Tin Tác Giả: @Model.Name</h6>
            </div>
            <div class="card-body p-4">
                <div class="row">
                    <div class="col-md-12 mb-4">
                        <h4 class="fw-bold text-dark">@Model.Name</h4>
                        <p class="text-muted">ID: @Model.Id</p>
                    </div>
                    
                    <div class="col-md-12">
                        <div class="mb-3 border-bottom pb-2">
                            <label class="small text-muted fw-bold text-uppercase">Năm Sinh</label>
                            <p class="h6 text-dark">@(Model.BirthYear?.ToString() ?? "---")</p>
                        </div>
                         <div class="mb-3 border-bottom pb-2">
                            <label class="small text-muted fw-bold text-uppercase">Quốc Tịch</label>
                            <p class="h6 text-dark">@(Model.Nationality ?? "---")</p>
                        </div>
                        <div class="mb-3 border-bottom pb-2">
                            <label class="small text-muted fw-bold text-uppercase">Ngày Tạo</label>
                            <p class="h6 text-dark">@Model.CreatedAt.ToString("dd/MM/yyyy HH:mm")</p>
                        </div>
                         <div class="mb-3">
                            <label class="small text-muted fw-bold text-uppercase">Tiểu Sử</label>
                             <div class="bg-light p-3 rounded">
                                <p class="mb-0 text-dark">@(Model.Biography ?? "Chưa có thông tin tiểu sử.")</p>
                             </div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="card-footer bg-transparent border-top py-3 d-flex justify-content-end gap-2">
                <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-primary">
                    <i class="bi bi-pencil me-1"></i> Chỉnh sửa
                </a>
            </div>
        </div>
    </div>
</div>